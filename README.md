# vid-dl

A CLI tool for video downloading (project skeleton).

## Structure

- **cmd/vid-dl/main.go** — CLI entrypoint
- **pkg/** — reusable Go packages
- **internal/** — private/internal project code
